#include <iostream>
using std::cin;
using std::cout;

/* What we did in class today was basically ../practice-problems/06/wc.cpp
 * I'll leave it to you to actually write the solution and test it out.
 * NOTE: the bonus part of that practice problem (wc.cpp) is probably
 * worth doing. */

int main()
{
	/* NOTE: in the following, we will NOT use cin, which by default will skip
	 * over spaces when you ask it to read a character.  */
	char c;
	while (scanf("%c",&c) == 1) {
	}
}

// vim:foldlevel=2
